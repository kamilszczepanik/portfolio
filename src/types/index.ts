export type Person = {
  id: number;
  name: string;
  designation: string;
  description: string;
  image: string;
  link: string;
};

export type Brand = {
  name: string;
  image: string;
  link: string;
};

export type Stat = {
  label: string;
  value: number;
  suffix: string;
};
